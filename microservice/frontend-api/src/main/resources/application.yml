spring:
  application:
    name: frontend-api
server:
  servlet:
    context-path: /api
  port: 7001
  compression:
    enabled: true

logging:
  level:
    org.springframework.*: debug
    com.heloword.*: debug
