spring:
  application:
    name: frontend-api
server:
  servlet:
    context-path: /api
  port: 7001
  compression:
    enabled: true

logging:
  level:
    org.springframework.*: debug
    com.heloword.*: debug

helo:
  llm:
    url: ${HELO_LLM_URL}
    model: gemma4:26b-a4b-it-q4_K_M
